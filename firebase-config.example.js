// Example Firebase Web App config. Copy to `firebase-config.js` (do NOT commit the real one).
// Values below are placeholders; replace with your actual project settings.
// Deployment note: Hosting build must include a non-ignored `firebase-config.js` file at runtime.
// If absent, Firestore calls will fail with missing configuration.

window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234567890:web:abcdef123456",
  // measurementId: "G-XXXXXXXXXX" // Optional
};
