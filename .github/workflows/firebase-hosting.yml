name: Disabled Firebase Workflow

on:
  workflow_dispatch: {}

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Firebase workflow disabled"

# This workflow is intentionally inert; Firebase is no longer used.
